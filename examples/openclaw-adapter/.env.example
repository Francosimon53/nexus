# Adapter server port
PORT=4300

# Public URL where this adapter is reachable (used in Agent Card)
AGENT_URL=http://localhost:4300

# Your OpenClaw agent's HTTP endpoint
OPENCLAW_AGENT_URL=http://localhost:3000

# Optional: NEXUS API key for SDK registration
NEXUS_API_KEY=
